# Server Configuration
server.port=5000
server.runtime.minutes=3
thread.pool.size=10
allocation.pool.size=4

# Timeout Settings
timeout.sign.ms=6000

# Audit Settings
audit.interval.sec=5
audit.file=audit_log.txt

# Resources
depot.count=3
vehicle.types=3
initial.vehicles.min=5
initial.vehicles.max=10

# Client Settings
client.request.interval.sec=3
client.confirm.delay.min.ms=2000
client.confirm.delay.max.ms=8000

default.distance.km=100
cancellation.penalty.percent=0.1